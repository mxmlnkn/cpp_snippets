language: cpp

compiler:
  - gcc

script: "nvcc thrust_sort/example.cu"

before_script:
  - sudo apt-get update -qq
  - sudo apt-get install -qq build-essential
  - sudo apt-get install -qq nvidia-cuda-toolkit nvidia-cuda-dev
